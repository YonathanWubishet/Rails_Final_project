<p id="notice"><%= notice %></p>

<h1>
<%= "***************************".center(56) %>
</h1>
<h1>
<%= "Welcome to Readzy's E-Book Browser." %>
</h1>
<h1>
<%= "***************************".center(56) %>
</h1>
<h1>
Books
</h1>

<table>
  <thead>
    <tr>
      <!--
      <th>Title</th>
      <th>Author</th>
      <th>Genre</th>
      <th>Publisher</th>
      <th>Published on</th>
      <th>Isbn</th>
      <th>Price</th>
      -->
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @books.each do |book| %>
      <tr>
        <td><%= book.title %></td>
        <!--
        <td><%= book.author %></td>
        <td><%= book.genre %></td>
        <td><%= book.publisher %></td>
        <td><%= book.published_on %></td>
        <td><%= book.isbn %></td>
        <td><%= book.price %></td>
        -->
        <td><%= link_to 'Details', book %></td>
        <td><%= link_to 'Read', book.file %></td>
        <td><%= link_to 'Download', book.file, download: '' %></td>
        <!--
        <td><%= link_to 'Edit', edit_book_path(book) %></td>
        <td><%= link_to 'Destroy', book, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        -->
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<!--
<%= link_to 'New Book', new_book_path %>
-->
